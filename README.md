## Prerequisites

- iCloud Drive sync enabled and `.secrets` downloaded
- VPN

## Installation

```bash
# Install Xcode Command Line Tools when prompted (git is included)
git clone https://github.com/K0R0BKIN/dotfiles.git ~/dotfiles
zsh ~/dotfiles/install.sh
```
